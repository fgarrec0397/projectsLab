import { createTheme } from "@ui/theme";

const theme = createTheme({
    palette: {
        mode: "dark",
        background: {
            default: "#000000",
        },
    },
});

export default theme;
